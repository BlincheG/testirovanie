<div class="row">
	<% @shops.each do |shop| %>
	<div class="col-md-4 cafe-preview">
	<p align="center"><%= image_tag shop.image.url(:medium) %></p>
		<strong>
			<p align="center">
				<%= link_to shop.name, shop_path(shop) %>
			</p>
		</strong>
	</div>
	<% end %>
</div>
